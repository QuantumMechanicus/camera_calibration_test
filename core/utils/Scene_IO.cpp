//
// Created by danielbord on 1/26/18.
//
#include "Scene_IO.h"

namespace scene_serialization {
    /*std::regex SceneFiles::rgx_left_keypoints(
            "(Left keypoints:|left keypoints:)(.*)");

    std::regex SceneFiles::rgx_right_keypoints(
            "(Right keypoints:|right keypoints:)(.*)");

    std::regex SceneFiles::rgx_left_info(
            "(Left camera info:|left camera info:)(.*)");

    std::regex SceneFiles::rgx_right_info(
            "(Right camera info:|right camera info:)(.*)");

    std::regex SceneFiles::rgx_left_intr("(Left intrinsics:|left intrinsics:)(.*)");

    std::regex SceneFiles::rgx_right_intr(
            "(Right intrinsics:|right intrinsics:)(.*)");

    std::regex SceneFiles::rgx_left_extr("(Left extrinsics:|left extrinsics:)(.*)");

    std::regex SceneFiles::rgx_right_extr(
            "(Right extrinsics:|Right extrinsics:)(.*)");

    std::regex SceneFiles::rgx_fundamental(
            "(Fundamental matrix:|fundamental matrix:)(.*)");

    std::regex SceneFiles::rgx_motion("(Relative motion:|relative motion:)(.*)");*/


}
